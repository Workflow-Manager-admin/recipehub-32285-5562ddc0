import RecipeList from "../components/RecipeList";

// PUBLIC_INTERFACE
export default function HomePage() {
  /**
   * Server component for the root page.
   * Renders the RecipeList client component.
   */
  return <RecipeList />;
}
